import React from 'react';
import { Story, Meta } from '@storybook/react/types-6-0';
import { {{pascalCase namespace}}, I{{pascalCase namespace}} } from '.';

export default {
  title: 'Components/{{pascalCase namespace}}',
  argTypes: {},
} as Meta;

const Template: Story = (args: I{{pascalCase namespace}}) => {
  return <{{pascalCase namespace}} {...args} />;
};

export const Default = Template.bind({});

Default.args = {};
