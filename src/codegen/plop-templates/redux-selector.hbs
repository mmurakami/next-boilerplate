import { ReduxState } from '../store';

// TODO: add {{camelCase namespace}} to ReduxState
export const select{{pascalCase namespace}} = (state: ReduxState): {{state_type}} => state?.{{camelCase namespace}};
